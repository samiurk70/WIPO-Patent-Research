# Exact versions used to produce the figures, tables and numbers in the manuscript.
# Regenerate the pinned set from a clean venv:  pip install <pkgs> && pip freeze
pandas==3.0.3
numpy==2.4.4
matplotlib==3.11.0
requests==2.33.1
scipy==1.17.1
openpyxl==3.1.5
